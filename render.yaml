services:
  - type: web
    name: ai-testfixer
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    autoDeploy: true
    healthCheckPath: /
    envVars:
      GROK_API_KEY: "GROK_API_KEY_PLACEHOLDER"
      BACKEND: "grok"
      PORT: "8501"
