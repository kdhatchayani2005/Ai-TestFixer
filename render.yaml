services:
  - type: web
    name: ai-testfixer
    env: docker
    dockerfilePath: Dockerfile
    autoDeploy: true
    healthCheckPath: /_stcore/health
    envVars:
      - key: GROK_API_KEY
        sync: false
      - key: BACKEND
        value: grok
      - key: PORT
        value: "8501"
